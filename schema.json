{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "chittytrace /health response",
  "description": "Response contract for GET /health as implemented in src/index.js.",
  "type": "object",
  "properties": {
    "status": { "type": "string", "enum": ["ok"] },
    "service": { "type": "string", "enum": ["ChittyTrace - Flow Analyzer API"] },
    "hyperdrive": {
      "type": "boolean",
      "description": "True when the CHITTYOS_CORE_DB Hyperdrive binding is present."
    }
  },
  "required": ["status", "service", "hyperdrive"],
  "additionalProperties": false
}
